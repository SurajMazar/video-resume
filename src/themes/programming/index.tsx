import React, { PropsWithChildren } from 'react'

const Programming: React.FC<PropsWithChildren<{}>> = (props) => {
  /**
   * COMPONENT PROPS
   */
  const { children } = props

  return <></>
}

export default Programming
